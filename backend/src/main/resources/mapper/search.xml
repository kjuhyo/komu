<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.kpop.dao.SearchDao">

	<select id="getSingerByName" parameterType="string" resultType="SingerDto">
		select * from singer 
		where singer_name like #{singer_name} 
		order by singer_like_cnt desc;
	</select>
	
	<select id="getSongByTitle" parameterType="string" resultType="SongDto">
		select * from song 
		where song_name like #{song_name};
	</select>
	
	<select id="getSongBySingerName" parameterType="string" resultType="SongDto">
		select * from song 
		where artist_name_basket like #{artist_name_basket};
	</select>
</mapper>