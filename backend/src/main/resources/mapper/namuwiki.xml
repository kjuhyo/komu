<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.kpop.dao.NamuDao">
	<insert id="insert" parameterType="NamuwikiDto">
		insert into namuwiki(uid, song_id, singer_id, namu_title, namu_content, namu_date, namu_img)
		values(#{uid}, #{song_id}, #{singer_id}, #{namu_title}, #{namu_content}, now(), #{namu_img})
	</insert>
</mapper>